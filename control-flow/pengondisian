<?php
  // PENGONDISIAN/PERCABANGAN
  // if else
  // if else if else
  // ternary = mengganti if else sederhana
  // switch = meringkas if else yang terlalu banyak

  $x = 30;
  if( $x < 20 ) {
	  echo "Benar";
  } else if ( $x == 20 ) {
	  echo "Bingo!";
  } else {
	  echo "Salah";
  }

?>
